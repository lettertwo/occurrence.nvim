version: 2
updates:
  # Maintain dependencies for GitHub Actions
  - package-ecosystem: github-actions
    directory: "/"
    schedule:
      interval: weekly
    open-pull-requests-limit: 10
    groups:
      gh-actions:
        patterns:
          - "*"
    commit-message:
      prefix: "ci"
      include: "scope"
